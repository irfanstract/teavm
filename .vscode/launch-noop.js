// No-op launcher used by the "Run arbitrary command (prompt)" launch configuration.
// The real work happens in the preLaunchTask; this script immediately exits.
console.log('noop');
process.exit(0);
